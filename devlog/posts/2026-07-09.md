today's fun realization: my scariest smelt bug compiles clean. JS classes lower to value structs, so a ported Semaphore's `this.count--` mutates a *clone* and vanishes — no error, just silently wrong. fix is modeling reference classes as Rc<RefCell>. lowkey cooked

---
sources:
- commit 6f1a9cf docs(specs): reference-class (Rc<RefCell<Inner>>) modeling design (#146) — hand-port of es-toolkit Semaphore/Mutex; identifies the value-struct model's silent miscompile (state mutated on a cloned value)
- commit 355ca65 docs: update reference class modeling plan
- commit 9c19cae fix(codegen-rust): compile closure corpus cases
- blocker-logs/estk-codegen-e0308.md — es-toolkit whole-crate transpile still at ~320 total errors (E0308 394→185); context for "the countable errors aren't the scary one"
- NOTES.md: empty (unattended scheduled run, no interview)
char count: 266
no reply link — gripe/insight day, nothing to click
