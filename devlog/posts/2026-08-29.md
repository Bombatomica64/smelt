benchmarked the generated rust vs plain node today and got humbled — sum_by is 49x slower lol. we emit Rc<RefCell<Vec>> for every list even ones never aliased, hash twice per map lookup, clone for no reason. spent the day ripping that out. es-toolkit fails 105->92

reply: chasing every one of these gaps down here: https://github.com/Bombatomica64/smelt

---
sources:
- git window: HEAD=bce1877, prior state last_commit (4672b60) is no longer in this branch's history, so drew from today's commits (2026-08-29).
- commits: #234 elide guarded default insert; #233 one hash/no alloc per Map/Set slot lookup; #232 stop no-op clones; #230 pointer-compare shared JS strings; #229 borrow at &T args + build entry default only when key absent; #228 stop building out-of-range value per index read + release profile that inlines; #225 one field store per object, shared strings, choosable allocator; #227/#226/#224 es-toolkit failures 105->98->96->92; #222/#221 callback by-shared-ref ABI + fast property hashing.
- blocker-logs/beating-node-plan.md: bench table — sum_by 48.9x slower than TS, unconditional Rc<RefCell<Vec>> is the smell; tiered Vec / Rc<[T]> / Rc<RefCell<Vec>> plan.
- devlog/NOTES.md: no pending notes.
- unattended scheduled run: interview skipped.
- char count: 264
