deleted 8 name-matched codegen "fixups" today. turns out they were quietly miscompiling real code — a user fn named `batch` got its None default rewritten to 0.0, didn't compile. the special cases WERE the bug. also cleared 6 es-toolkit blocker classes with general rules

reply: also stood up a proper docs site + landing this week if you wanna poke at it: https://github.com/Bombatomica64/smelt

---
sources:
- commit fix(frontend-ts,codegen): lower parameter defaults generally, drop name-matched fixups — deleted 8 name-matched default fixups (flat_*, to_title_case*, batch, ...) that corrupted ordinary user code; a user fn named `batch` had its None default rewritten to 0.0 → non-compiling Rust; flat(x) was silently running depth 0.0 instead of 1
- commit feat(frontend-ts,codegen): es-toolkit blocker wave 1 — cleared 6 es-toolkit probe blocker classes via general rules (interface method calls, constructor values, heterogeneous Map entries, exported consts, indexOf/includes fromIndex); frontend-ts 692 tests (+18), clippy zero warnings
- commits feat(site): documentation site w/ GitHub Pages + foundry-only landing (animated title, live demo, origin story) — basis for the reply link
- NOTES.md: no pending notes
- window: last_commit in state.json unreachable in this clone, fell back to last-24h git log per SKILL.md
- unattended scheduled run: no interview
- char count: 273 (main post, ≤280)
