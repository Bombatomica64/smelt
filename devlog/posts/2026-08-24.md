smelt dev log: callbacks finally lower to real rust generics (F: Fn(..) + ?Sized) not erased dyn. shipped 6 bugs doing it tho. 2 came from one loop where precedence was just whatever order the if-blocks sat in. rewrote it as a total match so that class of bug cant recur

reply: code's here: https://github.com/Bombatomica64/smelt

---
sources:
- git window: 64a3bd09..0ee635f (state.json last_commit → origin/main HEAD), Aug 23 → Aug 24. Busy two days after a quiet stretch.
- the story: plan #197, the callback-generics campaign. Increments 0/0b/1/2/3 landed (547f21a, 7df49db, b4be755, 895a05b, fd68ee3): borrowed callbacks now emit real generics `F{n}: Fn(..) + ?Sized`, a callback-only type parameter is inferred, composite generic returns get substituted. This is the project's north star — prefer `T: Trait` over runtime erasure (SmeltUnknown).
- the gripe/harden arc: the campaign shipped 6 defects; 2 came from one loop in the static-call argument ladder where precedence lived only in the order the if-blocks appeared (a monomorphizing passthrough tested before the borrowed-callback branch, and a param that was both a monomorphizing composite and mutable). Fixed at the time by hoisting branches + comments, but "a comment is a warning, not a guarantee." #206 (d3c2c77 / 1592c95) rewrote it as emitter/static_call_args.rs: StaticArgumentKind names every outcome in precedence order, exhaustive match, NO wildcard arm. Byte-identical on all three corpora (es-toolkit 745 files, remeda 391, radash 18). Also surfaced a THIRD untotalized ladder that nearly made a precedence test vacuous.
- supporting hardening: #204 (d9e8b86) seam assertions where the emitter can disagree with itself; #205 (3c94f27) made the shape corpus permanent + generated the rest. cargo test 855 passed / 0 failed on #206.
- devlog/NOTES.md: empty Pending (unattended scheduled run, no interview).
- char count: 270 / 280
- status: DRAFT — not posted. Copy-paste to X if you like it.
