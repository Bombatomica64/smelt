smelt dev log: two bugs today where the "recorded root cause" was just wrong. blamed virtual timers for the debounce fails — nah, codegen emitted a dead empty closure and called THAT, so debouncedFunc() never ran. also super() was getting `continue`'d over. 44 dead stubs gone

reply: code's here if you wanna audit the lowering pass with me: https://github.com/Bombatomica64/smelt

---

- chars: 276 / 280
- unattended scheduled run: no interview, drafted from git history + blocker-logs (NOTES.md empty)
- supporting: commit bf2e0e6 (dispatch calls on callable objects through their call slot — misdiagnosed as a virtual-timer/clock defect; real cause was coercion.rs fabricating an empty closure stub, so debouncedFunc() never ran; 44 live call-site stubs removed from es-toolkit crate). commit 3a92639 (lower super(...) + constructor param defaults — super calls were explicitly `continue`'d past in the constructor loop, dropping base init; previously-recorded cause "const-valued DOMException base" was wrong)
- AI angle: both were misdiagnosed root causes in generated lowering — the job today was auditing/disproving the recorded cause, not writing new code
- status: DRAFT — not posted. Copy-paste to X if you like it.
