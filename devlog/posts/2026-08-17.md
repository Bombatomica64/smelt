remember me crediting 'the bot refreshing probe reports' the last two logs? it was lying. rust-cache brought back .git dirs w/o worktrees, so the probe skipped all 12 clones + auto-committed 'everything stopped transpiling' for 9 commits. es-toolkit was fine, now at 871 passing

reply: the real work the lie was hiding: SmeltArray had value semantics so it silently dropped writes, `finally` got skipped when the try returned, and f === f was false across the erasure boundary. 12 fixes total. https://github.com/Bombatomica64/smelt

---
sources (unattended scheduled run — no interview, NOTES.md empty):
- window: devlog branch tracker `last_commit` c3fe6fa, `last_run` 2026-08-13. Window c3fe6fa..558996c (main HEAD), 33 commits, ~half pure chore(ci) noise.
- main-post gripe: 79e81ee fix(ci): stop the library probes publishing fabricated all-failed reports (#188) — the daily probe job checked libs into `target/library-probes` and skipped the clone when `<dest>/.git` existed; `Swatinem/rust-cache` restores `target/` and brings those `.git` dirs back without worktrees, so from 2026-08-07 every run skipped all 12 clones, scanned 0 files, and auto-committed a report claiming every library stopped transpiling. Nine commits of that landed on main over real data (es-toolkit was actually at 802 passing on 08-06). This directly recontextualizes my last two "quiet day, bot refreshing reports" posts (2026-08-04, 2026-08-13) — that bot was fabricating.
- main-post win: 7bdd67c Clone/equality campaign es-toolkit 823 → 871 passing (#191) — twelve transpiler fixes, zero regressions.
- reply detail (all from #191 body): 04851ce SmeltArray had value semantics (silently dropped writes, non-terminating on self-referential input); d7061d3 `finally` skipped when the try body returned; b907387 `f === f` false across the erasure boundary (JS function identity lost); b7376b3 every error class shared one boolean marker; 835ee62 symbol-tag loss made list-concat return an empty list.
- sibling CI-integrity fix not in post (same theme): 4aa3e5c fix(specialize): make sandbox tests fail closed instead of skipping silently (#186) — 22 sandbox security tests skipped with `return Ok(())` when bwrap/runtime missing, so the fail-closed boundary reported green in CI while never launching a guest. Also surfaced a 30s→0.41s orphan-reap hang. Cut the main post for length; picked the probe-report gripe because it ties to my own past posts.
- other window work: 48fc479 byte-buffer host objects get real bytes/identity/arguments; 8a0728c synthesize Map.prototype on an erased Map; 034753f Object.create mints a fresh object; 64dc39d golden HIR dumps + Python corpus (#190).
- char count: main post 278 / 280, reply 246. main post has no external link (per skill); repo link lives in the suggested first reply.
- status: DRAFT — not posted. Copy-paste to X if you like it.
