smelt 0.1.1 is on crates.io. the actual work was 90% build jank — split the gui into its own workspace, pinned get-size2 so the standalone build stops eating itself, wrangled gui libs into CI just to publish. also finally killed the last radash TS blockers. ship it

reply: https://github.com/Bombatomica64/smelt

---
sources:
- commits (2026-07-16 window):
  - ed80abb release: prepare crates.io 0.1.1
  - a95273f Make the ty-backed Python frontend publishable (#158)
  - aa580ec Finish Radash TypeScript transpilation blockers (#156)
  - 2a58b0d build: split smelt-gui into its own workspace + faster dev loop
  - 94d06b9 build(gui): pin get-size2 to 0.10.1 so the standalone workspace builds
  - 88f3066 ci: install GUI libraries for crate publishing
  - 93b7955 docs: correct crates.io installation instructions
  - (plus the usual pile of `chore(ci): update coverage report` noise commits, ignored)
- blockers: blocker-logs/radash-*.md (callable-prop-state, dynamic-constructor,
  callable-alias, etc.) — the batch that #156 closed out
- notes: devlog/NOTES.md had nothing pending
- interview: none — unattended scheduled run, drafted from git history
- char count: 267 (main post, ≤ 280)
