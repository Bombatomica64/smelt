quiet stretch on smelt, mostly ci noise. one real fix: nightly shape-grid went red bc `npx --yes typescript@5 tsc` looks for a bin named `typescript` that doesn't exist — pkg only ships `tsc`, so it died before tsc ran. classic npx footgun, nothing to do with the compiler lol

---
sources:
- window: b0c4ab8..origin/main (4 commits since 2026-09-13 run)
- commit d4d296e "fix(ci): run tsc via npx --package in the shape-grid workflow (#255)" — the one non-noise change; nightly shape-grid failed with "could not determine executable to run" because `npx --yes typescript@5 tsc` asked npx for a `typescript` bin (doesn't exist; pkg ships `tsc`/`tsserver`). Fix names the package and puts the bin after `--`; verified type-checks all 467 dumped grid programs at exit 0.
- other 3 commits: chore(ci) library-probe / coverage report refreshes — pure noise, skipped.
- devlog/NOTES.md: empty (no user notes)
- unattended scheduled run — no interview
- char count: 278 (single post, ≤280; no reply link — quiet/gripe day)
