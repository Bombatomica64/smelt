smelt dev log: today's bug — generated TS answered arr[-1] with the LAST element, not undefined. the emitter was applying python's negative-index rule to javascript. no panic, no diagnostic, just a plausible wrong value. silent bugs are the worst kind. es-toolkit 1014/45.

reply: code's here if you wanna dig in: https://github.com/Bombatomica64/smelt

---

- chars: 272 / 280
- window: state fallback — stored last_commit (4672b60) is unreachable on this branch and last_run was 2026-06-22, so drew from the recent es-toolkit arc + newest feature commit
- headline: b149994 (#247) "A JavaScript `arr[-1]` is not `arr[arr.length - 1]`" — Python's negative-index normalization was emitted for every language; JS `arr[-1]` is `undefined`, not the last element
- gripe: silent wrong value — no panic, no diagnostic; the fix needed an IR change (rule is a property of the site's source language), not an emitter switch. src: blocker-logs/estk-js-negative-index.md
- number: es-toolkit generated suite at 1014 passed / 45 failed (up from 909/150 at the start of the pass; arc: es-toolkit 87 -> 46 failing across #237,#241,#242,#244,#245,#246)
- other recent feature work: #240 (property-store key fingerprint), #243 (alias array at erased boundaries), #238/#236/#235/#234 (MIR list-escape + dict fusions)
- notes: NOTES.md pending was empty this run
- interview: skipped — unattended scheduled run
- status: DRAFT — not posted. Copy-paste to X if you like it.
