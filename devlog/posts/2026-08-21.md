smelt dev log: today was function-value ABI bugs. a callback that throws, passed as a value, dropped its Result and silently panicked — one type disagreement across 4 render sites. also landed a "generic binding engine" that changes zero output yet, dormant on purpose. we move

reply: code's here if you wanna point and laugh: https://github.com/Bombatomica64/smelt

---

- chars: 277 / 280
- window: recorded last_commit (4672b60) is gone from history and the strict 24h window is only a CI probe-report commit, so drafted from the last real work window (Aug 19–20).
- wins: #201 (throwing callback's error reachable by caller's catch), #195 (catch clause runs inside a closure body), #194 (loop stays a loop with try/catch/await in body), #200/#193 (callback-generics increment 1 + nine es-toolkit runtime defects)
- gripe: blocker-logs/estk-throwing-callbacks.md — the emitted Rust type for a Function value must agree with its MIR (erased-rest, may_throw, Future) at every render site; 4 sites disagreed, so a thrown error got swallowed into an abort. every symptom downstream of one type mismatch.
- dormant engine: blocker-logs/estk-callback-generics-plan.md + #200 — generic binding analysis added but deliberately byte-identical output, waiting on the Rust AST emitter migration before it changes anything.
- notes: NOTES.md pending was empty (unattended scheduled run, no interview)
- status: DRAFT — not posted. Copy-paste to X if you like it.
