hono crate finally at 0 errors after round 34. the fix that got it there — teaching smelt that js `function foo(){}` hoists AND can call itself — instantly surfaced 3 more latent emitter bugs. it compiles now but the generated tests still won't even parse. we're kinda back

reply: what smelt is, if you're new here: https://github.com/Bombatomica64/smelt

---
sources:
- commit 187610a (#258) "Hono round 34 + phase 3 round 1: crate at 0 errors and links, tests wired in, vitest globals, suite families" — the real story; landed 2026-09-23. New `lowering::hoisting` module (js function-declaration hoisting + self-reference); fixing it made 3 latent emitter defects reachable (nested-block decl collapsing to `SmeltUnknown::Null`, getter-only writeback hard-error, polymorphic `this` return typing) + raised the es-toolkit ratchet by 4, all fixed by general rules.
- blocker-logs/library-probes.md (2026-09-24): hono transpiles / Rust crate emitted, but generated `cargo test` "pass/fail counts could not be parsed" — the "tests won't even parse" gripe. (es-toolkit 1053/6, radash 384/3; rxjs/valibot/immer + all Python libs still don't transpile.)
- last 24h of git history was pure CI noise (chore(ci) library-probe + coverage report refreshes), so the substantive window is #258 from the day before. NOTES.md pending inbox was empty.
- unattended scheduled run: no interview; drafted from git history + blocker logs.

char count (main post): 277 / 280
