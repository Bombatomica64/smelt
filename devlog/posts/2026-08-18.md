es-toolkit 823→871 passing. then i saw what i was fixing: SmeltArray had value semantics (writes silently dropped), `f === f` false across the erasure boundary, `finally` not running on return. half of it was passing on bugs canceling out. radash still won't compile lol

reply: the clone/equality campaign, if you want the gory details: https://github.com/Bombatomica64/smelt

---
sources:
- PR #191 "Clone/equality campaign: es-toolkit 823 → 871 passing" (7bdd67c) — twelve transpiler fixes off the four largest failing groups, zero regressions
- b907387 preserve JS function identity across erasure boundary (`f === f`)
- 04851ce give SmeltArray JS reference semantics (was dropping writes / non-terminating on self-referential input)
- d7061d3 run `finally` when the try body returns
- b7376b3 / 835ee62 error-class discrimination + symbol-tag preservation
- gripe: PR #191 notes radash compile failure predates this branch and is still unfixed (many open radash-* blocker-logs)
- NOTES.md: empty (unattended scheduled run, no interview)

char count: 272 (main post, excludes reply line)
