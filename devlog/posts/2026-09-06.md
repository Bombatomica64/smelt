es-toolkit: 45 failing rows down to 4. the twist — the last 4 arent even smelt bugs. isBrowser wants a real DOM, isPlainObject wants cross-realm. everything that WAS a bug got a general lowering rule, zero per-lib hacks. built the whole js object model for it. basically cooked

reply: and the flex: avoidable SmeltUnknown went DOWN doing this (34072→32912). adding a whole js-object runtime tier usually means MORE erasure, not less. arrays-are-objects, prototypes in an identity table, new f() actually constructs. https://github.com/Bombatomica64/smelt

---
sources:
- git window: 6e12774..464addd (devlog state.json last_commit → code tip), 2026-09-04 → 2026-09-06. 65 commits, heavy on agent worktree-merge chrome but the story is one campaign.
- the headline: #248 (31f4866) "es-toolkit 45 -> 4: twenty-nine general rules for the last failing rows" — es-toolkit generated suite 1014/45 → 1055/4 (41 resolved, 0 newly failing). remeda 1789/0, radash 84/84. The 4 remaining rows are genuinely not lowerable bugs: isBrowser (needs a DOM, out of scope), isPlainObject cross-realm (deferred D3), `at` (deferred D2), mergeWith (deferred D1). See blocker-logs/estk-final45-plan.md.
- the object-model tier that made it possible (all landed this window, all general rules per the north star): "A JavaScript array is an object with named properties" (42ea72e5), "Model the prototype link once, in the identity-keyed table" (8927d3ce) / "A recorded prototype belongs in an identity-keyed table" (559df422), "A JavaScript function is an object, and `new f()` is a construction" (66b9f96b), "an ordinary method call supplies its receiver" (97e68bbf this-binding), "Object.create(p) remembers p" (25c29fa0), regex pattern translation by grammar (749f5d03), vitest asymmetric matchers as a value deep-equality consults (be501c43), async closure future adapted/awaited/forwarded (4cb87cb9). Runtime preludes added: asymmetric_matcher, builtin_member, byte_buffer.
- the flex I led the reply with: avoidable SmeltUnknown ratchet re-snapshotted 34072 → 32912 (a DECREASE) while adding an entire runtime tier — usually adding dynamic machinery raises avoidable erasure. This is the project's north star holding: concrete types first, erasure only at real dynamic boundaries.
- gripe angle in the main post: closing a 45→4 campaign sounds like pure win, but the last 4 are the ones a lowering rule can't touch — they want host capabilities (a browser DOM) or cross-realm semantics. Campaign's basically over not because we crushed everything, but because what's left isn't ours to fix.
- devlog/NOTES.md: empty Pending (unattended scheduled run, no interview).
- reply line: repo link included — points at real clickable work (#248 + the object-model commits).
- char count: main post 279 / 280, reply 271.
- status: DRAFT — not posted. Copy-paste to X if you like it.
