smelt was rebuilding the whole array on every push: a list aliased from an erased object's field re-wrapped its buffer each time, so the push hit a dead copy — saw 2 elements where js says 3. callgrind said 87% of 20B instrs were memcpy. slow AND wrong

reply: https://github.com/Bombatomica64/smelt

---

## thread alternative (if you want the whole week)

1/ smelt was rebuilding the whole array on every push: a list aliased from an erased object's field re-wrapped its buffer each time, so the push hit a dead copy — saw 2 elements where js says 3. callgrind said 87% of 20B instrs were memcpy. slow AND wrong

2/ also killed a `typeof` that emitted a 9-arm match allocating a fresh String at every site (isEqualWith did 3 in a row to answer one enum discriminant). now one shared &'static str helper — 23 inlined tables in 20 files collapse to one call, crate 9KB smaller

3/ and a new showcase: a hono + node:sqlite todo api transpiled to rust. point isn't the app, it's the dep — express ships js with separate @types, but hono publishes its own ts, so smelt eats the whole framework as input alongside it

---

sources:
- git window: 464addd..b0c4ab8 (devlog state.json last_commit → fresh origin/main HEAD), 2026-09-06 → 2026-09-13. Real work under the usual CI-noise chrome (library-probe / coverage report commits).
- headline (main post): #253 (25ed2e0) "Write an erased object's list field back as an alias, not a rebuild". A push whose receiver is a list aliased from an erased object's field emitted a write-back that rebuilt the whole array every push (`SmeltUnknown::Array(items.clone()...map(into_smelt_unknown).collect())`) instead of the aliasing re-wrap (`SmeltUnknown::Array(items.clone().into())`, an Rc refcount bump). It was a correctness bug, not just slow: rebuilding the buffer while keeping the `id` detached the field from the local's storage, so a push through the local hit a dead copy — new fixture `a_local_alias_of_an_erased_object_field_stays_one_array_across_pushes` sees 2 elements where JS says 3. Found by callgrind on remeda `unique_by`: 87% of 20.5B Ir in memcpy / drop_in_place / clone / into_vec, `into_vec` alone issuing 300,120,003 clones. O(n) per push = O(n²) per pipeline.
- thread 2/ : #252 (107588c) "Lower `typeof` to a shared `&'static str` helper". typeof over an erased value was a nine-arm SmeltUnknown match inlined at every site, each arm building a fresh String; isEqualWith produced three back to back. Now `smelt_typeof(&SmeltUnknown) -> &'static str` in the runtime prelude, gated on the crate lowering an `Rvalue::TypeofValue`; typeof-only locals carried as `&'static str` not String. es-toolkit: 23 inlined tables in 20 files → one helper + 23 calls; generated crate 9.2 KB smaller; tests 2503→2507.
- thread 3/ : #254 (7a7fe39) "examples: Hono todos CRUD showcase app". `examples/typescript/hono_crud/` — strict-TS todo API on Hono 4 + node:sqlite, fetch-framework sibling of the Express showcase. The point is the dependency: Express ships JS with separate @types/express, but Hono publishes its own TypeScript sources, so Smelt transpiles the framework itself as input alongside the app.
- also in window (didn't fit): #251 (1da8f50) unbreak the es-toolkit benchmark build — `entry_clone_deep(...).unwrap()` was stale after clone_deep_471 started returning SmeltUnknown instead of Result; dropping the `.unwrap()` was the whole fix. Codegen improved, the harness didn't keep up.
- devlog/NOTES.md: empty Pending — unattended scheduled run, no interview.
- reply line: repo link included — main post points at real clickable work (#253).
- char count: main post 252 / 280. Thread: 1/ 252, 2/ 246, 3/ 232.
- status: DRAFT — not posted. Copy-paste to X if you like it.
