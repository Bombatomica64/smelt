found a fun one: smelt turned js `arr[-1]` into the last element. that's the python rule — in js it's just a missing key, so `undefined`. no panic, no diagnostic, plausible wrong value, the worst kind. the index normalizer was speaking python to every language.

reply: real instance was es-toolkit's `at.ts`, which normalizes negatives itself then hits `-1` and must miss — smelt wrapped a second time and answered `'c'`. fix threads source-language down HIR→MIR so a negative subscript follows the rule of its own file. https://github.com/Bombatomica64/smelt

---
sources:
- commit b149994 "A JavaScript `arr[-1]` is not `arr[arr.length - 1]`" (#247) — shared Rust-emitter index normalizer applied Python's `len + index` wrap to every language; JS negative subscript is a missing property key → `undefined`, only `.at(-1)` counts from the end. Fix adds `NegativeIndex` (FromEnd/OutOfRange) to `smelt_mir::Place::Index` and `file: FileId` to `smelt_hir::SourceFile` so the site's source language is known at emit; Python unchanged, `.at()` keeps its wrap via `OptionalIndex`.
- context: also landed recently — es-toolkit failing tests ground 47→46 (ambient `declare` value binding, #246), property-store key fingerprints (#240).
- NOTES.md: empty (no user notes this window).
- run: unattended scheduled run, no interview. Recorded `last_commit` (4672b60) was unreachable (rebased/squashed away); windowed on fresh 2026-09-04 work instead.

char count (main post): 263
