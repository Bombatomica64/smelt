smelt's been quiet — a week of just the bot refreshing probe reports. last real fix was two bugs that both had the wrong root cause written down: super() silently `continue`d out of derived ctors, callable objects never hitting their call slot. both just plain bugs.

---
sources:
- window: devlog branch tracker `last_commit` 077e4e3, `last_run` 2026-08-04. Window 077e4e3..c3fe6fa (HEAD).
- quiet-week gripe: the only non-noise commits in the window are the two Aug-7 fixes + the Aug-8 coverage doc; everything since (Aug 9–13) is `chore(ci): update library probe report`. No human commits in ~a week.
- 3a92639 feat(frontend): lower super(...) and constructor parameter defaults — `super(...)` was explicitly `continue`d out of the ctor statement loop, dropping base init; the previously recorded cause ("base is a const-valued DOMException expr") was disproved.
- bf2e0e6 fix(codegen): dispatch calls on callable objects through their call slot — callable-object calls emitted a dead stub, so `debouncedFunc()` never ran; the `toHaveBeenCalledTimes` failures were the assertion correctly reporting nothing happened, not a timer defect.
- context: 3fb8e7d docs(testing) coverage analysis (Aug 8) — 74.1% lines, branch cov 63.6% is the weak axis; NOTES.md empty; unattended scheduled run, no interview.
- char count: 268 (≤280, single post). no reply link (reflective/quiet day, nothing single to click).
