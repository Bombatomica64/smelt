smelt dev log: `super(...)` in derived ctors was being explicitly `continue`d — every subclass silently dropped its base init. and the cause i'd written down was flat wrong. half this job is still auditing whatever an agent did to my lowering pass. we move.

reply: code's here if you wanna point and laugh: https://github.com/Bombatomica64/smelt

---

- chars: 259 / 280
- window: state last_commit (4672b60) unreachable on this branch; used recent-history fallback. last_run 2026-06-22. today (08-09) is just CI probe-report noise, so the post covers the last real work (08-07/08-08).
- wins: 3a92639 (lower super(...) + constructor param defaults; new lowering/decls/super_call.rs), bf2e0e6 (dispatch calls on callable objects through their call slot — killed 44 dead call-site stubs so debounce/throttle actually fire), 3fb8e7d (workspace test-coverage analysis)
- gripe: the recorded cause for the super() bug was wrong — blamed a const DOMException base, disproved with a plain base class where super(message) dropped just the same. the "auditing AI-written lowering" theme continues.
- notes: NOTES.md pending was empty this run
- status: DRAFT — not posted. Copy-paste to X if you like it.
