got smelt transpiling hono's own TS alongside the app now, not just @types stubs — 2nd real showcase after express.

gripe: known-shape objects still lower to string-keyed hashmaps, so ~43% of a hot loop is malloc + siphash. object model's next. cooked but we're so back

reply: https://github.com/Bombatomica64/smelt

---
sources:
- 7a7fe39 examples: Hono todos CRUD showcase app (#254) — Hono publishes its own TS, so Smelt transpiles the framework as input alongside the app (unlike Express' JS + separate @types)
- 31f4866 es-toolkit 45 -> 4 via 29 general rules (context for the object-model push)
- blocker-logs/struct-shaped-objects.md — known-shape objects lower to Dict(String, union) not a typed struct; callgrind on partition/10k records: ~43% malloc/free/drop, 16.2% SipHash
- NOTES.md: empty this run
- unattended scheduled run, no interview
- char count: 272 (single post, ≤280)
