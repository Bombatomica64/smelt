smelt dev log: spent the day getting erased list/array/set bindings + fn refs to keep stable identity through lowering (33→30 fails). then tried the bigger undefined rearchitecture, it regressed 30→39, reverted it. some days the deliverable is a revert. we move.

reply: code's here if you wanna point and laugh: https://github.com/Bombatomica64/smelt

---

- chars: 262 / 280
- run: unattended scheduled run — no interview; drafted from git history + NOTES (NOTES pending was empty)
- window: 4672b60..7ab4031 (cluster B "representation rearchitecture")
- wins: ade26b5 + 7ab4031 (reference identity for erased list/array/set bindings, 33→30), d1f2c7e (stable fn-reference identity via per-item transpile-time accessors), b442631 (regex literals → RegExp values), a18867a (Number.isNaN/isFinite/isInteger → predicate closures), 2a43a7a (parseInt radix), 21b1a08 (leading-rest tuples → lists), 7ec8fc9 (Object.keys excludes internal marker keys)
- gripe: fc5e3de — B2 distinct-undefined attempted then reverted; the literal-only slice regressed 30→39. needs undefined at every producer + library nullish reconciliation, a multi-round effort not a single pass
- deferred: a1482d1 — list value-flow cases (tap, sets, nested arrays, isIncludedIn data-last, reduce) still need option 2a; 68040c9 — mergeDeep fn-identity remaining
- status: DRAFT — not posted. Copy-paste to X if you like it.
