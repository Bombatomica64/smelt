remeda compat grind. `=> void` callbacks were extracting erased values into unit and panic!ing at runtime — js just throws the result away, we didnt. tap() + vi.fn mocks both cooked. also `new Date("2016/03/10")` quietly returned NaN so nothing ever compared equal lol

reply: https://github.com/Bombatomica64/smelt

---
sources:
- commit eb44110 — Discard erased values coerced to void (=> void callbacks panic!ing at runtime; tap/vi.fn fixed); parse slash/dash Date strings (new Date("2016/03/10") returned NaN)
- commit d1f2c7e — stable function-reference identity via per-item transpile-time accessors (isDeepEqual)
- commit ade26b5 — reference identity for erased list/array bindings (cluster B1 step 2)
- commit b442631 — lower regex literal args to RegExp values not source strings
- commit a18867a — Number.isNaN/isFinite/isInteger as predicate closures
- commit 2a43a7a — honor radix in parseInt / Number.parseInt
- commit fc5e3de — gripe candidate: B2 distinct-undefined attempted, regressed 30→39, reverted (deferred, not used in final post to keep it tight)
- blocker-logs/: nothing new in window (all dated 2026-06-10/11)
- devlog/NOTES.md: empty (no pending notes)
- interview: skipped — unattended scheduled run
- window: 4672b60..HEAD (28 commits, CI coverage noise filtered)
char count: 268 (main post, reply line excluded)
