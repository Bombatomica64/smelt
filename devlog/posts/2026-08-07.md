two bugs today i'd both blamed on the wrong thing. the debounce/throttle "timer bug" was really codegen emitting a dead empty-closure stub, so the fn never ran. and super() wasn't mishandled — the ctor loop just skipped right over it, dropping base init. skill issue lol

reply: fixes are up if you want the gory details: https://github.com/Bombatomica64/smelt

---
sources:
- bf2e0e6 fix(codegen): dispatch calls on callable objects through their call slot — calling a callable object emitted a dead `SmeltErasedFunction { .. Null }` stub, so debounce/throttle never ran; the toHaveBeenCalledTimes failures were correct. Blamed on virtual timers/clock first (wrong). Fixed by coercing records w/ `__smelt_call` through the call slot; all 44 live stubs gone.
- 3a92639 feat(frontend): lower super(...) and constructor parameter defaults — ctor loop did `if is_super_call { continue }`, dropping every derived-class base init; also ctor params used the type default not the real default. Prior recorded cause ("const DOMException base") was disproved.
- NOTES.md: empty (no user notes this window)
- unattended scheduled run: no interview
- window: last_commit 4672b60 not in this fresh clone, fell back to last-24h git log (HEAD 12bf467)
- char count (main post): 272
