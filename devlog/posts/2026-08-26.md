smelt dev log: es-toolkit 150→109 failures, every fix a general rule not a per-lib hack. best one: union method calls were quietly erasing to SmeltUnknown — the concreteness check bailed on the exact u32::MAX sentinel a union carries. two halves built to meet, never met till now

reply: code's here: https://github.com/Bombatomica64/smelt

---
sources:
- git window: 0ee635fb..9cdb35ed (state.json last_commit → origin/main HEAD), Aug 24 → Aug 26. 23 commits, mostly real feature work under CI-noise chrome.
- the headline: es-toolkit generated suite failures fell 150 → 109 over two campaigns. #209 (68cdd00) took 150 → 115 across nine root causes (rest-slot arity veto so a same-named variadic export stops lending its `...rest` to a fixed-arity `omit` — the bug where `delete result[key]` deleted nothing; Object.is number equality so NaN===NaN; named failing assertion + source location in generated suites). #214 (391f68b) took 115 → 109 on two more: null/undefined no longer collapse into one Option empty state at the element-type join, and `.call` stops passing thisArg as a positional arg where the callee already has a receiver slot. All general lowering rules, zero per-library special cases — the north star.
- the gripe/story I led with: #213 (252c644) `dispatch union-receiver method calls statically`. A method call whose receiver was a union of classes was lowered *dynamically* — receiver `into_smelt_unknown`, method looked up as an object field, invoked through the erased SmeltUnknown ABI — even though every arm is a class declaring the method and the tagged enum is right there. Exactly the "reconcile concrete union arms" erasure the project rules single out; it even infected the signature (dynamic ABI is fallible → return wrapped in Result). Both halves of the machinery already existed (resolve_method returns ItemId(u32::MAX) for a union, emitter's union_method_text matches the tag and calls each arm's concrete method) and never met: the concreteness check ends in `item.0 != u32::MAX`, the exact sentinel a union deliberately carries, so a perfectly concrete union failed the test and fell through to erased lowering.
- Python side momentum (secondary, didn't fit the post): #211 (259bf76) result-lib blockers 22→14 — `T = TypeVar(...)` now a declaration not a rejected call, `@property` getters lower to Descriptor getter methods, class metadata; #212 (167c896) bind declared keyword arguments; also inherited methods on subclasses, `super().__init__()` lowering, instance-field declaration; #210 (769e2c8) oxc → 0.147.
- devlog/NOTES.md: empty Pending (unattended scheduled run, no interview).
- reply line: repo link included — real clickable work (#209, #213, #214, #211).
- char count: 279 / 280
- status: DRAFT — not posted. Copy-paste to X if you like it.
