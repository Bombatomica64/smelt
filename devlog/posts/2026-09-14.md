nightly shape-grid check was red thanks to a pure npx footgun: `npx typescript@5 tsc` makes npx hunt for a bin named `typescript`, which the pkg doesn't ship (it's `tsc`). exits 1 before tsc even runs. `--package typescript@5 -- tsc` and 467 gen'd programs type-check clean again

---
sources:
- commit d4d296e — fix(ci): run tsc via npx --package in the shape-grid workflow (#255). `npx --yes typescript@5 tsc` failed with "could not determine executable to run" because the `typescript` package has no `typescript` bin; fixed to `npx --yes --package typescript@5 -- tsc`. Verified: type-checks all 467 dumped grid programs under `--strict --noEmit --target es2020`, exit 0.
- window: last_commit in state.json (4672b60) is not present in this clone (history diverged / squash-merged), so fell back to the recent window per SKILL. Everything else since is nightly CI noise (library-probe + coverage report refreshes).
- NOTES.md: empty (no pending notes).
- unattended scheduled run: no interview.
- no `reply:` line — reads as a war-story/gripe, not a go-click-this; skipping the link per SKILL ("don't link reflexively").

char count: 279 (≤ 280, single post)
