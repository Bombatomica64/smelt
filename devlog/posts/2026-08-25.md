python day. cursed one: smelt lowered classes with no annotated fields to zero fields, so every self.x silently became the class itself. self.inner.a() looked up on the wrong type and fell through. fixed + subclass methods now emit compiling rust. params still can't infer tho

reply: https://github.com/Bombatomica64/smelt

---
sources:
- commit 3105238 fix(frontend-py): declare instance fields assigned in __init__; drop the ty spike — the fieldless-class bug where `field_type` falls back to the receiver, so every `self.<name>` silently took the class's own type (issue #94)
- commit f302aef fix(py,codegen): inherited methods on subclasses; lower super().__init__() — codegen only emitted each class's own methods, so `&B` calling an inherited `fetch` failed to compile
- commit e957fa7 deps(py): upgrade Ruff/ty to 0.0.10; infer nested closure return types
- blocker-logs/python-frontend-gaps.md — remaining hole: unannotated *parameter* types (`def inc(x):`) still error; ty resolves from annotation only, not defaults or call sites. 123 occ across 4 libs, "not a version-bump away" = the "params still can't infer tho" gripe
- NOTES.md pending inbox was empty; drafted from git history (unattended scheduled run, no interview)

char count: 276 (≤ 280 ✓)
