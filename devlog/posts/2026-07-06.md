smelt dev log: const inlining was a hand-maintained whitelist of ~20 expr kinds, everything else died with "shape not supported". ripped it out for one exhaustive mapper. also: one bad es-toolkit spec aborts the WHOLE crate build. closed ~5 blocker families today.

reply: fixes are here if you wanna dig: https://github.com/Bombatomica64/smelt

---

- chars: 264 / 280
- window: state last_commit (4672b60) no longer exists in this repo (prior ephemeral branch); fell back to today's commits (2026-07-06)
- theme: es-toolkit lowering blocker families closed so the whole-crate build stops aborting
- wins: #128 const-item computed-initializer inlining (whitelist -> exhaustive try_map_child_exprs, 29 spec files); #129 named-callback arity (30 files); #131 @ts-expect-error suppression in overload resolution + live toHaveProperty on erased actuals (11 files); #132 six array/callback misc gaps; #133 host globals as first-class values + ES2022 Error cause options
- gripe: one unbuildable spec aborts the entire crate build (blocker-logs/estk-overload-resolution.md, estk-const-item-inlining.md); es-toolkit compat suites deliberately call fns with too few/wrong args under @ts-expect-error to probe runtime coercion, and the resolver bailed instead of falling back to the impl signature
- notes: NOTES.md pending was empty this run
- interview: none (unattended scheduled run)
- status: DRAFT — not posted. Copy-paste to X if you like it.
