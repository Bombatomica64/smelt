cursed codegen bug today: es-toolkit pull/pullAt/remove were "mutating" a clone. the &mut got dropped at the call site so the callee wrote into a throwaway. fix needed monomorphized vs erased args handled the exact opposite way. &mut invariance my beloved. 791->802 passing

reply: https://github.com/Bombatomica64/smelt

---
sources:
- commit 6d9ec04 "fix(codegen): keep in-place list mutation visible to ordinary callers" — the day's one real content commit. es-toolkit pull/pullAt/remove take `&mut SmeltList<..>`, but the call site fell into `mutable_reference_argument_text`'s by-value `else` branch (place_ty != target), so the callee mutated a clone. Monomorphized args (generic T bound from the argument) and erased args (`SmeltList<SmeltUnknown>`, no reborrow under &mut invariance) needed opposite handling.
- es-toolkit suite: 791/268 -> 802/257 (11 resolved, 0 newly failing). pull_spec 3->0, pullAt_spec 6->0, remove_spec 3->1.
- gripe thread from the commit body: shared closure captures need a temp so the RefCell guard isn't alive across the callee (re-entry would panic); avoidable SmeltUnknown ticked +1 and the es-toolkit ratchet was re-snapshotted deliberately.
- NOTES.md: empty (no user notes this run).
- unattended scheduled run — no interview; drafted from git history.
- char count: 273 (main post, within 280).
