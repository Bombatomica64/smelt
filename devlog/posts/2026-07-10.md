dragged es-toolkit through the rust backend today: JS classes as Rc<RefCell<Inner>>, a new SmeltJsSet, killed a pile of E0308/E0277 clusters. then pointed smelt at date-fns and it instantly ate dirt on `let x: T = {}` — module globals only take literal inits rn. cooked

reply: https://github.com/Bombatomica64/smelt

---
sources:
- commits (2026-07-10): #147 model shared-mutable JS classes as Rc<RefCell<Inner>>, #148 four E0308 clusters in es-toolkit transpile, #149 SmeltJsSet container + tail E0277 fixes, #151 lower empty statements as no-ops
- gripe: blocker-logs/date-fns-full-baseline.md (2026-07-10) — full corpus stops at `let defaultOptions: DefaultOptions = {}`; module-level mutable globals only accept literal initializers, so an empty typed record is rejected before Rust emission
- NOTES.md: empty (unattended scheduled run, no interview)
- char count: 271
