benchmarked smelt's generated rust against the typescript it came from. 872x slower lmao. not uniform overhead tho, just a few cursed lowering bugs: cloning the whole vec 3x per slice, Rc cycles leaking closures. fixed 3, now remeda `flat` runs 1.07x FASTER than its TS

reply: benchmark harness + the full findings writeup: https://github.com/Bombatomica64/smelt

---
sources:
- #216 "bench: TS vs generated-Rust benchmarks, and three lowering fixes they found" (163ddb3) — geomean 872x slower (es-toolkit) / 760x (remeda) across 32 paired cases, spread 1.1x–28,930x; fixes: borrow-only receivers (cloning backing Vec 3x per slice), self-recursive closures leaking via Rc cycle (now Weak), and the ABI-agreement follow-up. remeda `flat` 1.07x faster than TS; es-toolkit `flatten` peak RSS 1,779 MiB -> 4.7 MiB.
- #217 "es-toolkit 109 -> 107" (3e70392) — reference-identity + five other lowering defect classes (context, not the post's focus).
- unattended scheduled run: no interview; NOTES.md empty; state.json last_commit was stale/unknown so windowed on the last 24h.

char count: 269
