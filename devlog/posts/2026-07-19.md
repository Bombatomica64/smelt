js Map is a reference value. mine wasn't — SmeltJsMap derived Clone over a Vec, so a set() thru an expression hit a throwaway clone and lost the write. silently broke a cycle guard: circular inputs recursed forever, SIGABRT'd the test binary. fix: Rc<RefCell> so clone aliases

reply: how smelt transpiles a JS Map into Rust with real reference semantics: https://github.com/Bombatomica64/smelt

---
sources:
- commit 187f0b1 fix(codegen-rust): give SmeltJsMap JS reference semantics via shared Rc backing — the core story: Vec + derive(Clone) deep-copied entries, so a `map.set(k,v)` lowered as an expression mutated a throwaway clone. broke isEqualWith's cycle guard (`stack.set(a,b)` before recursing), 9 *circular* tests → stack overflow → SIGABRT. fixed with Rc<RefCell<Vec<..>>> so `.clone()` aliases the shared store.
- context: recent window is a multi-week run of "identity across erasure" work — 0e6052f/bab8dac/482add6 (Map identity, stages 2a/2b) and 0c03322 (Set identity, stage 3), plus 1de42f3 (resume short-circuit join at shared tail) which restored the deep recursion that made this bug reachable.
- NOTES.md: empty (no user notes this run).
- unattended scheduled run — no interview.
- char count: 278 (main post, ≤280 ✓)
