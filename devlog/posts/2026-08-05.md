quiet stretch on smelt. last real fix: es-toolkit's pull/pullAt were mutating a *clone* of your array, not the array — the write got lost at the call site. since then it's just the probe bot running solo: es-toolkit 802/257, half the libs still won't even transpile

reply: https://github.com/Bombatomica64/smelt

---
sources:
- git: 6d9ec04 "fix(codegen): keep in-place list mutation visible to ordinary callers" (Jul 31) — es-toolkit pull/pullAt/remove mutated a cloned SmeltList; the &mut place was dropped at the call site because place_ty != target, so the callee mutated a clone. Needed opposite treatment for monomorphized (generic, pass through unconverted) vs erased (convert-in-place adapter) callees.
- git window: recorded last_commit 4672b60 is not present in this fresh clone, so the window fell back to recent history. Everything since Jul 31 is CI noise — daily `chore(ci): update library probe report` / coverage-report commits, no substantive code.
- blocker-logs/library-probes.md (regenerated 2026-08-05): es-toolkit transpiles, generated suite 802 pass / 257 fail. radash transpiles (counts unparsed). ts-pattern, valibot, neverthrow, immer, rxjs (TS) and returns, result, more-itertools, funcy, toolz (PY) still do not transpile — dominant blocker "non-working Rust" lowering gaps.
- devlog/NOTES.md: empty (unattended scheduled run, no interview).
- char count: 267 (main post, within 280)
