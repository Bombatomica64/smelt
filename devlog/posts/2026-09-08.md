es-toolkit: 45 failing rows -> 4, all general rules, no special-casing.

the real one: codegen was rebuilding a whole array on every push to an aliased erased field, and the rebuild detached storage so the push hit a dead copy. slow AND wrong. now a refcount bump.

---

- chars: 264 / 280
- window: state was stale (last_commit 4672b60 gone from history, last_run 2026-06-22); today (09-08) is CI-only noise, so drew from the 09-06/09-07 real-work window
- wins: 25ed2e0 #253 (aliased erased-object list field written back as an alias, not a rebuild — was a correctness bug: push went to a dead copy), 31f4866 #248 (es-toolkit 45->4 via 29 general rules), 107588c #252 (typeof lowered to shared &'static str helper instead of an allocating 9-arm match)
- gripe: 1da8f50 #251 — es-toolkit bench build broke because codegen improved out from under the harness (clone_deep now returns SmeltUnknown, not Result); 4 es-toolkit families still deferred (Type::Null, optional index reads, tagged-realm constant eval)
- notes: devlog/NOTES.md pending was empty this run
- interview: skipped — unattended scheduled run
- status: DRAFT — not posted. Copy-paste to X if you like it.
