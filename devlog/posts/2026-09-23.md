hono crate finally at 0 cargo errors after 34 rounds (started at 883 lol). today's gremlin: smelt wasn't hoisting `function foo(){}` so a decl used before its line silently collapsed to SmeltUnknown::Null — compiled fine, emitted garbage. now it's a real Rc<dyn Fn>

reply: the whole thing transpiles TS/Python to Rust by hand-quality lowering: https://github.com/Bombatomica64/smelt

---
sources:
- commit 187610a (#258): "Hono round 34 + phase 3 round 1: crate at 0 errors and links, tests wired in, vitest globals, suite families" — the nested `function foo(){}` hoisting + self-reference fix; nested-block decl used before its position was *silently wrong* (fell through to the forward-callable fallback, lowered to an empty erased record, call collapsed to `SmeltUnknown::Null`) and now emits a concrete `Rc<dyn Fn(String) -> String>` — a net removal of erasure. Fixture `109_nested_function_declaration_hoists`.
- context: #256 (Hono rounds 1-33) took the crate 883 -> 3 cargo errors; #258 closed the last 3 to reach 0 + links.
- blocker-logs: standards-hono-first-cargo-check.md, standards-hono-headers-init-resolved.md (Hono campaign trail).
- NOTES.md: empty (no user notes this run).
- unattended scheduled run: no interview; drafted from git history + logs.

char count (main post): 267
