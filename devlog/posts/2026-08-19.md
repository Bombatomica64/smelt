clone/equality tail. two instances of DIFFERENT classes compared EQUAL in deep-eq bc both .constructor read undefined lol. then wrote an architecture review that graded my last one: every rec needing to move state got skipped. god object lives, files just regrouped

reply: also today: camelCase props on any-typed objects silently vanished (wrote some_prop, read someProp — found it chasing RegExp.lastIndex), and the 11 typed arrays were all one Vec<f64>. es-toolkit 871->873 https://github.com/Bombatomica64/smelt

---
sources (unattended scheduled run — no interview, NOTES.md empty):
- window: devlog tracker `last_commit` 558996c, `last_run` 2026-08-17. Window 558996c..main HEAD (4d15304), 3 commits: one real PR (#192) + 2 chore(ci) probe-report refreshes (noise).
- everything below is #192 "Clone/equality tail, typed-array views, module-scope constructors, and the second architecture pass (Phases 1 + 4)":
- main-post bug: `feat(classes): per-class constructor identity on erased instances` — es-toolkit `isEqualWith` gates instance comparison on `.constructor`; an erased instance couldn't name its class so `.constructor` read `undefined`. Both sides `undefined` → `Object.is(undefined, undefined)` held → two instances of DIFFERENT classes compared EQUAL. False positive in a deep-equality primitive.
- main-post gripe: `docs: architecture plan for the second pass` — successor to the 2026-06-04 review, and it scores the old one first: every recommendation with a single seam to build got built (SmeltUnknown coercion seam, unified expr emitter, Rvalue operand algebra); every one that needed moving state or a source-of-truth did not. ModuleBuilder went 98→63 fields and 18 numbered files→33 concern-named ones — files regrouped while the god object survived. Phase 4 this PR started the state split: extracted ClassRegistry, LocalScope, InterfaceRegistry off ModuleBuilder (63→44 fields).
- reply detail: `fix(objects): key erased field writes on the source property name` — assigning a property on an erased receiver stored it under the Rust-mangled symbol (`some_prop`) while reads used the source name (`someProp`), so any camelCase prop written to an any/unknown object was silently lost. Found chasing `RegExp.lastIndex` (an instance of the same bug). Now uses `symbol_source_name`.
- reply detail: `feat(typed arrays): real view identity with element typing` — the 11 numeric typed arrays were all modeled as one plain `Vec<f64>`; now real ArrayBuffer/view identity with a `TypedArrayElement` type. es-toolkit 871→873 passing (fixes flattenObject TypedArrays, isEqual different buffers, isTypedArray). Noted ratchet cost: +29 avoidable-erasure (documented in blocker-logs/estk-typed-array-views.md), not cut into the post.
- other window work not in post: `feat(classes): lower module-scope constructor functions` (`function Foo(){this.a=1}` + `new Foo()` at module top level now lowers); typed-array SmeltUnknown-ratchet doc resolution; Type::Host phase-2 implementation spec.
- char count: main post 265/280, reply 244. main post has no external link (per skill); repo link lives in the suggested first reply.
- status: DRAFT — not posted. Copy-paste to X if you like it.
