smelt dev log: hono crate finally at 0 cargo errors, 883 → 0 over 34 rounds. last one was a hoisting bug: js binds `function foo()` for its whole scope, smelt lowered it at its textual spot so `return step()` before the decl was dead code MIR dropped. quiet since, mostly CI noise

reply: code's here if you wanna point and laugh: https://github.com/Bombatomica64/smelt

---

- chars: 280 / 280
- window: recent-history fallback — state.json last_commit 4672b60 is stale (dated 2026-06-22) and no longer exists in this repo, so fell back to recent history
- wins: #256 (Hono rounds 1-33, crate 883 → 3 cargo errors), #257 (fix CI/runtime-tier after #256), #258 (Hono round 34 + phase 3 round 1 — crate at 0 errors, tests wired in, vitest globals)
- gripe / detail: the hoisting knot in #258 — a `function foo()` declaration lowered at its textual position, so a use before the decl landed after a `return` and MIR dropped it as dead code, leaving a call to a name nothing declares. new `lowering::hoisting` module answers order once (decl lowers just before first sibling that mentions its name)
- last 2 days (09-24, 09-25): only `chore(ci): update library probe report` commits — pure noise
- notes: NOTES.md pending was empty this run
- status: DRAFT — not posted. Copy-paste to X if you like it.
