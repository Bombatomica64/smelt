gave every record entry a u32 key fingerprint so same-length keys skip a full memcmp before matching. deep_equal 1.2x, memcmp in sum_by down 54%. meanwhile es-toolkit's still 96 tests red and every "one bug" family i crack open is secretly 8 separate roots. whack-a-mole

reply: https://github.com/Bombatomica64/smelt

---
sources:
- commit bcca042 (#240) — runtime: give every property-store entry a key fingerprint. u32 `(len<<16)|(first<<8)|last` per SmeltFieldEntry compared before full key eq; deep_equal 1.200x, `__memcmp_avx2_movbe` in sum_by falls 54%, entry grows 56->64 bytes.
- commit d45dad3 (#246) — es-toolkit 47->46: ambient `declare` must not create a value binding (context on the correctness side of the day, not in the post).
- blocker-logs/estk-remaining-triage.md — es-toolkit generated suite 963 passed / 96 failed; clone/cloneDeep "family" measured as 8 independent roots, not 1 (per estk-clone-cluster.md); several other families each split into multiple roots.
- devlog/NOTES.md — empty (unattended scheduled run, no interview).

char count: 270 (main post, excl. reply line)
