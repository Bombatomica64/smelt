es-toolkit generated-rust failures: 92 -> 47. nearly every bug is the same shape — values losing their guts crossing the type-erasure seam. class methods, promises, __proto__, globalThis all vanish. fix one spelling, find the next. the seam is my nemesis rn

reply: erasure-seam grind lives here → https://github.com/Bombatomica64/smelt

---
sources:
- commits (2026-08-30): f90a39c es-toolkit 87->58 (promises, `this`, sort, equality, erased calls), aa71336 58->55 (globalThis, absent erased slots, structural class eq), d9fb527 55->53 (class methods across the erasure seam), 5db12bb 53->51 (Object.defineProperties, method shorthand, Number.isSafeInteger), 6ece674 51->47 (__proto__ accessor, dropped optional record field). earlier ec6c222 92->87 (host values lost content crossing erasure). recurring gripe visible across the messages: the erasure boundary keeps eating value content.
- 92 is the starting count from ec6c222's window; 47 is the current es-toolkit failure count after #245.
- today's literal window (2026-09-01) was CI noise only (library-probe report refresh); substantive work was the 2026-08-30 grind above. state.json last_commit was a bad object, so fell back to recent history.
- NOTES.md: empty (no pending notes). Unattended scheduled run — no interview.
- char count: 259
