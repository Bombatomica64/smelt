the array write-back i'd filed as "just slow" turned out to be a correctness bug — push through a local hit a dead copy, js says 3 you get 2. also killed typeof building a fresh String at every site (isEqualWith did it 3x in a row). now one &'static str helper, zero allocs.

reply: https://github.com/Bombatomica64/smelt

---
sources:
- commit 25ed2e0 (#253) — "Write an erased object's list field back as an alias, not a rebuild": rebuild-per-push detached the field from the local's storage, so a push through the local hit a dead copy; new fixture saw 2 elements where JS says 3. Correctness bug, not just slow.
- commit 107588c (#252) — "Lower `typeof` to a shared `&'static str` helper instead of an inlined allocating match": nine-arm SmeltUnknown match inlined at every site, each arm allocating a String; isEqualWith emitted three back to back. es-toolkit: 23 inlined tables in 20 files collapse to one prelude helper, no site allocates.
- commit 1da8f50 (#251) — "Unbreak the es-toolkit benchmark build: drop a stale `.unwrap()`": codegen now returns SmeltUnknown not Result; stale .unwrap() broke the whole bench crate. (context/gripe, didn't make the post)
- devlog/NOTES.md: empty (no user notes this window).
- Unattended scheduled run — no interview.

char count: 276
