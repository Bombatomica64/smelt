today's whole vibe: finding out CI was lying to me 3 ways. probe job auto-committed fake 'everything broke' reports for 9 commits, a real run got thrown out on a non-fast-forward push, n 22 sandbox tests 'passed' by skipping with return Ok(()). green checks meant nothing lol

---
sources:
- 79e81ee fix(ci): stop the library probes publishing fabricated all-failed reports (#188) — rust-cache restored bare `.git` dirs, so every run skipped all 12 clones, scanned 0 files, and auto-committed "every library stopped transpiling". 9 fake-regression commits landed on main.
- de84049 fix(ci): rebase the probe report onto the branch tip before pushing (#189) — a full 10-min probe run of all 12 libs was thrown away because the coverage job pushed to main 3 min earlier → non-fast-forward reject. fix: pull --rebase --autostash.
- 4aa3e5c fix(specialize): make sandbox tests fail closed instead of skipping silently (#186) — 22 sandbox specialization tests skipped via `return Ok(())` when bwrap/prlimit/guest missing, indistinguishable from a pass. fail-closed security boundary was green while never launching a guest. new `prereq` module + a `sandbox` CI job.
- blocker-logs/library-probes.md — once the real probe ran again: es-toolkit at 823/236 passing generated tests.
- NOTES.md: empty (no user notes this window).
- unattended scheduled run — drafted from git history + blockers, no interview.

char count: 275 (main post)
no reply line: CI/infra gripe day, nothing to click through to.
