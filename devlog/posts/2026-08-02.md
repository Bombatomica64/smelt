smelt was funneling js `throw err` through a std::io::Error, so an erased Error came out as the literal string `[object Object]` — the catch then rebuilt a fake error record from that text. found it via 3 tests panicking with [object Object] as their own message

reply: payload survives now — added a typed SmeltThrown channel instead of stringifying. also killed a sibling bug where es-toolkit pull/remove mutated a clone of your array, not the array. https://github.com/Bombatomica64/smelt

---
sources (unattended scheduled run — no interview, NOTES.md empty):
- window: git log 7aceeb9..dfd7d54 since last log (2026-07-28), 42 commits, 18 of them pure chore(ci) noise
- throw payload bug (main post): de1b0490 feat(codegen): preserve thrown-value payloads through a typed exception channel — four sites (control_flow.rs:713/964, closures.rs:1261, call.rs:243) funnelled the value through format!/to_string into std::io::Error; erased JS Error is a marker-bearing SmeltUnknown::Object so Display gave `[object Object]`; three retry_spec tests panicked with it. Fix: new thrown.rs SmeltThrown { value } + smelt_throw/smelt_thrown_value/smelt_thrown_message
- family-7 report: 56e36e4f docs(blocker-logs) exception-payload; cb63afb9 refresh end-to-end fixtures for throw prelude
- list-mutation sibling bug (reply): 6d9ec045 fix(codegen): keep in-place list mutation visible to ordinary callers — es-toolkit pull/pullAt/remove took &mut but the call site handed array.clone().into(), mutating a clone; needs two opposite paths (monomorphized passes element through, erased needs conversion)
- other window work not in post: bfb68f18 closure loop header back-edge detection; dfa70fd9 marker-faithful clone/structuredClone (PR #184); f8b2db91 statement-position map.set coercion; 283e9bf3 clippy 86->0; d464ebfb/99ca75a3 CI trims + advisory clippy gate
- char count: main post 264, reply 223
