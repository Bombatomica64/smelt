# Generated by Cargo
# will have compiled files and executables
debug
target

# Local checkouts made by scripts/checkout_probe_libraries.py. Deliberately not
# under target/: the CI cargo cache restores target/ and would strip the probe
# worktrees while leaving their .git dirs behind.
/.library-probes/

# These are backup files generated by rustfmt
**/*.rs.bk

# MSVC Windows builds of rustc generate these, which store debugging information
*.pdb

# Generated by cargo mutants
# Contains mutation testing data
**/mutants.out*/

# RustRover
#  JetBrains specific template is maintained in a separate JetBrains.gitignore that can
#  be found at https://github.com/github/gitignore/blob/main/Global/JetBrains.gitignore
#  and can be added to the global gitignore or merged into this file.  For a more nuclear
#  option (not recommended) you can uncomment the following to ignore the entire idea folder.
#.idea/
.codex
codedb.snapshot
dist/
dist-smelt/

# Analysis/profiling scratch output — never track
.codex-tmp/
graphify-out/
crates/graphify-out/
perf.data
perf.data.old

# Investigation reports are scratch output; keep the directory, not the logs
blocker-logs/*.md
!blocker-logs/.gitkeep
# library-probes.md is CI-managed (library-probes.yml commits it)
!blocker-logs/library-probes.md
# smelt-unknown-report.md documents the SmeltUnknown metric methodology + baseline
!blocker-logs/smelt-unknown-report.md
# estk-const-item-inlining.md documents the const-item-inlining blocker fix
!blocker-logs/estk-const-item-inlining.md
# estk-overload-resolution.md documents the overload-resolution blocker fix
!blocker-logs/estk-overload-resolution.md
# estk-array-misc.md documents the array/callback misc blocker fixes
!blocker-logs/estk-array-misc.md
# estk-host-globals.md documents the es-toolkit host-global / Error-cause fixes
!blocker-logs/estk-host-globals.md
# estk-globalthis.md documents the dynamic globalThis[key] read + three singleton fixes
!blocker-logs/estk-globalthis.md
# estk-lowering-tail.md documents the small es-toolkit lowering tail blocker fixes
!blocker-logs/estk-lowering-tail.md
# estk-transpile-gate.md documents the five whole-crate transpile-gate blocker fixes
!blocker-logs/estk-transpile-gate.md
# estk-mir-gates.md documents the es-toolkit MIR-lowering gate fixes (await/assignment/postfix)
!blocker-logs/estk-mir-gates.md
# estk-module-globals.md documents module-level mutable state (thread-local mutable globals)
!blocker-logs/estk-module-globals.md
# estk-optional-receivers.md documents the optional-chained modeled-receiver desugar and probe families
!blocker-logs/estk-optional-receivers.md
# estk-host-override-slots.md documents bounded globalThis host-constructor reassignment
!blocker-logs/estk-host-override-slots.md
# estk-match-emitter.md documents the match/switch join-block emitter fix + first whole-crate transpile
!blocker-logs/estk-match-emitter.md
# estk-first-transpile-diagnostics.md is the generated-crate diagnostics report for the first whole-crate transpile
!blocker-logs/estk-first-transpile-diagnostics.md
# estk-codegen-misc.md documents the E0004/E0596/E0599 generated-Rust codegen fixes
!blocker-logs/estk-codegen-misc.md
# estk-codegen-e0308.md documents the systematic E0308 (mismatched types) emitter fixes
!blocker-logs/estk-codegen-e0308.md
# remeda-regression.md documents the remeda no-regression investigation (baseline vs current binary)
!blocker-logs/remeda-regression.md
# estk-codegen-tail.md documents the non-coercion structural/trait/name tail fixes
!blocker-logs/estk-codegen-tail.md
# reference-classes.md documents the Rc<RefCell<Inner>> reference-class feature
!blocker-logs/reference-classes.md
# reference-classes-diagnostics.md is the es-toolkit rust-diagnostics report after the feature
!blocker-logs/reference-classes-diagnostics.md
!blocker-logs/estk-e0308-round2.md
# estk-jsset-and-tail.md documents the SmeltJsSet container and tail E0277/E0631 fixes
!blocker-logs/estk-jsset-and-tail.md
# estoolkit-runtime-current.md is the es-toolkit runtime baseline plus the
# ranked root-cause family analysis follow-up agents pick their target from
!blocker-logs/estoolkit-runtime-current.md
# estoolkit-runtime-after.md is the same suite after the closure-loop fix
!blocker-logs/estoolkit-runtime-after.md
# estoolkit-runtime-family7.md documents the exception-payload ABI and the
# per-defect analysis of why the remaining throw assertions still fail
!blocker-logs/estoolkit-runtime-family7.md
# estoolkit-frontend-families-plan.md is the design plan for the hardest
# remaining es-toolkit families (clone/isEqual/merge + the in-place-mutation
# lever), including the corrected root causes follow-up agents work from
!blocker-logs/estoolkit-frontend-families-plan.md
# test-coverage-analysis.md documents workspace test coverage gaps and priorities
!blocker-logs/test-coverage-analysis.md
# estk-clone-and-equality.md records the clone/isEqualWith campaign and clusters
# the remaining failures by probe-confirmed root cause for follow-up agents
!blocker-logs/estk-clone-and-equality.md
# estk-class-identity-and-symbols.md records the class-identity / symbol-key pass
# of that campaign, including the root causes it deliberately did not attempt
!blocker-logs/estk-class-identity-and-symbols.md
# phase2-type-host-spec.md is the Phase 2 (`Type::Host`) implementation spec from
# docs/architecture-plan-second-pass.md finding B: site inventory, scope cut,
# dispatch fix, and staged checkpoints against the es-toolkit ratchet
!blocker-logs/phase2-type-host-spec.md
# estk-arguments-and-self-recursion.md documents the iterable-`arguments` and
# self-recursive named-function-expression fixes
!blocker-logs/estk-arguments-and-self-recursion.md

# Generated docs site output
_site/

# Agent worktrees are transient checkouts created under .claude/, never part of the tree
.claude/worktrees/

# Callback-generics lift design plan.
!blocker-logs/estk-callback-generics-plan.md

# Throwing-callback / function-value ABI investigation.
!blocker-logs/estk-throwing-callbacks.md
# estk-remaining-triage.md maps the remaining es-toolkit generated failures to root causes
!blocker-logs/estk-remaining-triage.md
# estk-js-negative-index.md documents the JavaScript negative-index read fix
!blocker-logs/estk-js-negative-index.md

# Probe library checkouts (see .github/compat/libraries.json for pinned refs)
third_party/es-toolkit/
third_party/radash/
third_party/remeda/

# Benchmark artifacts: bundled third-party TypeScript and raw result JSON,
# both regenerated by benchmarks/prepare.py and benchmarks/run.py.
benchmarks/ts/vendor/
probe.json
# estk-final45-plan.md + estk-final45-*.md: root-cause reports and the batch plan for the last 45 es-toolkit failures
!blocker-logs/estk-final45-plan.md
!blocker-logs/estk-final45-arrays.md
!blocker-logs/estk-final45-functions.md
!blocker-logs/estk-final45-objects.md
!blocker-logs/estk-final45-predicates.md
!blocker-logs/estk-final45-misc.md
!blocker-logs/estk-final45-async.md

# shared cargo target for the es-toolkit probe crate used by parallel implementer worktrees
/target-estk/

# private per-worktree cargo target dirs for parallel implementer agents
/target-priv/
/target-estk-priv/
# private cargo target dirs for the remeda/radash regression gates run locally
/target-rem-priv/
/target-rad-priv/
!blocker-logs/estk-final45-after.md
/target-gates/
!blocker-logs/estk-final45-host-gaps.md

# examples/typescript/express_crud is a real npm project: its installed
# dependencies and local SQLite file are never tracked (dist/ is covered above).
node_modules/
examples/typescript/express_crud/todos.db
!blocker-logs/express-v1-baseline.md
!blocker-logs/standards-tier-plan.md
!blocker-logs/standards-tier-progress.md
!blocker-logs/hono-campaign-plan.md
